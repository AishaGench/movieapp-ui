const initialState = {}

export default (state =[1,2,3], { type, payload }) => {
    switch (type) {
/*
    case typeName:
        return { ...state, ...payload }
*/
    default:
        return state
    }
    
}
